const Title = ({ children }) => {
  return (
    <h2 className="text-center text-stone-900 text-2xl font-bold uppercase md:text-[32px] lg:text-[40px]">
      {children}
    </h2>
  );
};

export default Title;
